# Claude Code tooling â€” not part of the website
.claude/
.mcp.json

# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.vscode/
.idea/

# Local scratch
*.log
scratchpad/

# Never commit real secrets. config.js IS tracked on purpose: it only holds
# public values (site URL, phone, and the Supabase anon key, which is safe to
# publish because Row Level Security enforces access). If you ever add a
# service_role key or an SMTP password, put it in one of these instead:
.env
.env.*
secrets.json

# Logo source masters (large originals, not served by the site)
brand/

# --- Next.js ---
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts


# Next.js / Vercel
.next/
out/
.vercel
next-env.d.ts

.env.local
.env*.local

# the template itself should be committed
!.env.example
